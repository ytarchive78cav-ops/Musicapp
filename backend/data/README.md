# Sample audio fixture

This repository intentionally does **not** commit binary audio files so GitHub/Codex PR creation remains compatible.

Generate the local sample fixture with:

```bash
python scripts/generate_sample_audio.py
```

It writes `backend/data/sample_melody.wav` for local testing.
